version: "3.8"
services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app
      - ./downloads:/app/downloads
    command: sh -c "pip install --no-cache-dir -r requirements.txt && python bot.py"
